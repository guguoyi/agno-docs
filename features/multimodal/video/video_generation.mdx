---
title: Video Generation Tools
sidebarTitle: Video Generation
description: Generate videos with AI tools in Agno agents.
keywords: [video generation, ai video, video tools, fal, replicate, video creation]
mode: wide
---

The following example demonstrates how to generate a video using `FalTools` with an agent. See [FAL](https://fal.ai/video) for more details.

## Prerequisites

The following example requires the `fal_client` library and an API key which can be obtained from [Fal](https://fal.ai/).

```shell
uv pip install -U fal_client
```

```shell
export FAL_KEY=***
```

```python video_agent.py
from agno.agent import Agent
from agno.models.openai import OpenAIResponses
from agno.tools.fal import FalTools

fal_agent = Agent(
    name="Fal Video Generator Agent",
    model=OpenAIResponses(id="gpt-5.2"),
    tools=[
        FalTools(
            model="fal-ai/hunyuan-video",
            enable_generate_media=True,
        )
    ],
    description="You are an AI agent that can generate videos using the Fal API.",
    instructions=[
        "When the user asks you to create a video, use the `generate_media` tool to create the video.",
        "Return the URL as raw to the user.",
        "Don't convert video URL to markdown or anything else.",
    ],
    markdown=True,
)

fal_agent.print_response("Generate video of balloon in the ocean")

```
## Developer Resources

- View a [Replicate](/features/multimodal/video/usage/generate-video-replicate) example.
- View the [Fal toolkit documentation](/integrations/toolkits/others/fal).
- View a [ModelsLabs](/features/multimodal/video/usage/generate-video-models-lab) example.
